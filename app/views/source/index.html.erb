<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport"
          content="width=device-width, 
                   initial-scale=1.0" />

    <% if @sources %>
    <title><%= @page.title %> Sources </title>
    <% end %>
  </head>

  <body class = "section">
    <div class = "container-fluid">
      <%= render 'layouts/navbar' %>
      <% if @sources %>
          <%= render 'layouts/sources-container' %>
          <%= render 'layouts/saved' %>
      <% end %>
    </div>
    <br>
</html>